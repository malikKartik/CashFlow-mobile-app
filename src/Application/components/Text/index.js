import React from 'react';
import {Text, StyleSheet} from 'react-native';

const Text = (props) => {
  const styles = styleSheet(props);
  return (
    <>
      <Text style={}></Text>
    </>
  );
};

const styleSheet = () => {
  StyleSheet.create({});
};

export default Text;
